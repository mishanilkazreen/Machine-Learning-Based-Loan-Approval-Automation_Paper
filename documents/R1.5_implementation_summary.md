# R1.5 Implementation Summary: Statistical Significance, Variance Analysis, and Ablation Studies

## Overview

This document summarizes the implementation of statistical rigor enhancements to address Reviewer 1, Comment 5 (R1.5) for the MSc research paper on ML-based loan eligibility prediction. The reviewer noted "insufficient experimental depth, including lack of statistical significance testing, variance analysis, and ablation studies."

**Implementation Scope:**
- Variance analysis across cross-validation folds
- Statistical significance testing between model pairs
- Ablation studies to isolate component contributions

**Status:** All three analysis modules have been successfully implemented and integrated into the Jupyter notebook.

---

## Implementation Details

### Location in Notebook
All new analysis code has been added to `code/juupteteryer.ipynb` in a new section titled "Statistical Rigor Analysis", positioned after model training and before the LIME interpretability section.

### Libraries Used
- **scipy.stats**: `ttest_rel` (paired t-test), `wilcoxon` (Wilcoxon signed-rank test)
- **sklearn.model_selection**: `StratifiedKFold`, `cross_val_score`
- **pandas**: DataFrame creation and formatting

### Modules Implemented

#### 1. Variance Analysis Module
**Function:** `compute_variance_analysis(models, X_train_sm, y_train_sm)`

**Purpose:** Reports mean ± standard deviation for all evaluation metrics across 5-fold cross-validation to demonstrate model stability.

**Key Features:**
- Uses StratifiedKFold with n_splits=5, shuffle=True, random_state=42
- Evaluates 4 models: Tuned RF, Tuned XGBoost, Stacking Ensemble, Voting Ensemble
- Computes 5 metrics: Accuracy, Precision, Recall, F1, AUC
- Formats output as "mean ± std" with 2 decimal places
- Stores per-fold F1 scores for reuse in significance testing

**Output:** DataFrame with models as rows and metrics as columns

#### 2. Statistical Significance Testing Module
**Function:** `compute_significance_testing(models, X_train_sm, y_train_sm, f1_scores_dict)`

**Purpose:** Performs paired statistical tests between all model pairs to determine which performance differences are statistically significant.

**Key Features:**
- Uses paired t-test (primary) and Wilcoxon signed-rank test (supplementary)
- Generates all 6 pairwise comparisons between 4 models
- Reuses per-fold F1 scores from variance analysis (avoids redundant computation)
- Formats p-values to 4 decimal places
- Marks statistically significant differences (p < 0.05) with asterisk (*)

**Output:** Pairwise comparison matrix showing p-values for both tests

#### 3. Ablation Study Module
**Function:** `compute_ablation_study(X_train, y_train, X_train_sm, y_train_sm, final_model)`

**Purpose:** Evaluates the pipeline with individual components removed to quantify contributions of SMOTE, hyperparameter tuning, and feature selection.

**Key Features:**
- Defines baseline (full pipeline: SMOTE + tuned RF + feature selection)
- Implements 3 ablation conditions:
  - **No SMOTE**: Uses original unbalanced data (X_train, y_train)
  - **No Hyperparameter Tuning**: Uses default RandomForestClassifier
  - **No Feature Selection**: Uses all 12 original features with SMOTE reapplied
- Computes delta (baseline F1 - ablation F1) for each condition
- Formats delta with sign: "-X.XX" (negative impact) or "+X.XX" (positive impact)

**Output:** Comparison table with baseline and three ablation conditions

---

## Variance Analysis Results

**Note:** Execute the notebook cells to generate actual results. The variance analysis will show:

- Mean ± standard deviation for each metric across 5-fold CV
- Low standard deviation indicates consistent performance across folds
- High standard deviation suggests performance varies with training data

**Expected Output Format:**
```
Model            Accuracy        Precision       Recall          F1              AUC
Tuned RF         X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX
Tuned XGBoost    X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX
Stacking         X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX
Voting           X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX
```

**Interpretation:**
- Compare standard deviations across models to assess stability
- Lower std indicates more reliable performance
- Use these results to support claims about model robustness in the paper

---

## Statistical Significance Results

**Note:** Execute the notebook cells to generate actual results. The significance testing will show:

- Pairwise comparisons between all 4 models (6 total pairs)
- P-values from both paired t-test (primary) and Wilcoxon test (supplementary)
- Asterisk (*) marks statistically significant differences (p < 0.05)

**Expected Output Format:**
```
                 Tuned RF                    Tuned XGBoost               Stacking                    Voting
Tuned RF         —                           t: p=X.XXXX, W: p=X.XXXX    t: p=X.XXXX*, W: p=X.XXXX   t: p=X.XXXX, W: p=X.XXXX
Tuned XGBoost    t: p=X.XXXX, W: p=X.XXXX    —                           t: p=X.XXXX, W: p=X.XXXX    t: p=X.XXXX*, W: p=X.XXXX
Stacking         t: p=X.XXXX*, W: p=X.XXXX   t: p=X.XXXX, W: p=X.XXXX    —                           t: p=X.XXXX, W: p=X.XXXX
Voting           t: p=X.XXXX, W: p=X.XXXX    t: p=X.XXXX*, W: p=X.XXXX   t: p=X.XXXX, W: p=X.XXXX    —
```

**Interpretation:**
- p < 0.05: Statistically significant difference (marked with *)
- p ≥ 0.05: No statistically significant difference
- Paired t-test is primary; Wilcoxon provides supplementary evidence
- Use significant differences to justify model selection in the paper

**Key Findings to Report:**
- Which model pairs show statistically significant differences
- Whether ensemble methods significantly outperform individual models
- Whether the differences justify the added complexity of ensemble approaches

---

## Ablation Study Results

**Note:** Execute the notebook cells to generate actual results. The ablation study will show:

- Baseline performance (full pipeline)
- Performance with each component removed
- Delta showing the contribution of each component

**Expected Output Format:**
```
Condition                      Accuracy        Precision       Recall          F1              AUC             Delta_F1
Baseline (Full Pipeline)       X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX     —
No SMOTE                       X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX     -X.XX
No Hyperparameter Tuning       X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX     -X.XX
No Feature Selection           X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX     X.XX ± X.XX     -X.XX
```

**Interpretation:**
- **Negative delta** (e.g., -0.05): Removing component decreased performance by 0.05 (component helped)
- **Positive delta** (e.g., +0.03): Removing component increased performance by 0.03 (component hurt)
- **Zero delta**: Component had no impact

**Key Findings to Report:**
- **SMOTE contribution**: How much did class balancing improve F1 score?
- **Hyperparameter tuning contribution**: How much did tuning improve over default parameters?
- **Feature selection contribution**: Did reducing features improve or hurt performance?

**Expected Insights:**
- SMOTE typically shows positive contribution (negative delta) for imbalanced datasets
- Hyperparameter tuning should show positive contribution
- Feature selection may show positive or negative contribution depending on feature quality

---

## Methodology Notes

### Cross-Validation Strategy
- **Method:** 5-fold stratified cross-validation
- **Configuration:** `StratifiedKFold(n_splits=5, shuffle=True, random_state=42)`
- **Rationale:** Stratified sampling maintains class distribution in each fold
- **Reproducibility:** Fixed random_state=42 ensures consistent fold assignments

### Statistical Tests Used

#### Primary Test: Paired t-test
- **Function:** `scipy.stats.ttest_rel`
- **Type:** Parametric test
- **Assumption:** Normally distributed differences
- **Use case:** Comparing mean F1 scores across paired CV folds
- **Significance level:** α = 0.05

#### Supplementary Test: Wilcoxon Signed-Rank Test
- **Function:** `scipy.stats.wilcoxon`
- **Type:** Non-parametric test
- **Assumption:** No distribution assumptions
- **Use case:** Provides supplementary evidence when normality is questionable
- **Significance level:** α = 0.05

**Why both tests?**
- Paired t-test is more powerful when assumptions are met
- Wilcoxon provides robustness when normality is violated
- Agreement between both tests strengthens conclusions

### Paired Testing Requirement
All models use identical CV fold assignments (same random_state) to enable paired comparisons. This is critical because:
- Paired tests compare performance on the same data splits
- Reduces variance from different train/test splits
- Increases statistical power to detect true differences

---

## Paper Integration

### Recommended Additions to Paper

#### Section: Experimental Setup (or Methodology)
**Add subsection:** "Cross-Validation and Statistical Testing"

**Content to include:**
- Description of 5-fold stratified cross-validation
- Explanation of paired statistical testing approach
- Justification for using both parametric and non-parametric tests
- Significance level (α = 0.05)

**Example text:**
> "To ensure robust evaluation, we employed 5-fold stratified cross-validation with fixed random seed (random_state=42) for reproducibility. Model performance was assessed using paired t-tests to determine statistical significance of differences, with Wilcoxon signed-rank tests providing supplementary non-parametric evidence. Significance was determined at α = 0.05."

#### Section: Results
**Add subsection:** "Model Performance and Variance Analysis"

**Content to include:**
- Variance analysis table (Table X)
- Discussion of model stability (standard deviations)
- Comparison of consistency across models

**LaTeX table generation:**
```python
# In notebook, after running variance analysis:
print(variance_results_df.to_latex(index=False, float_format="%.2f"))
```

**Add subsection:** "Statistical Significance of Model Comparisons"

**Content to include:**
- Significance testing matrix (Table Y)
- Identification of significant differences
- Discussion of which models significantly outperform others
- Justification for model selection based on statistical evidence

**LaTeX table generation:**
```python
# In notebook, after running significance testing:
print(significance_results_df.to_latex(index=True, float_format="%.4f"))
```

**Add subsection:** "Ablation Study: Component Contributions"

**Content to include:**
- Ablation study results table (Table Z)
- Quantification of each component's contribution
- Discussion of which components provide the most value
- Justification for pipeline design choices

**LaTeX table generation:**
```python
# In notebook, after running ablation study:
print(ablation_results_df.to_latex(index=False, float_format="%.2f"))
```

#### Section: Discussion
**Add discussion points:**
- Interpretation of variance analysis (model stability)
- Implications of statistical significance findings
- Insights from ablation study about pipeline design
- Comparison with related work that may lack such rigorous analysis

---

## Files Modified

- `code/juupteteryer.ipynb`: Added Statistical Rigor Analysis section with 3 modules
